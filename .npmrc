# next-auth@5.0.0-beta.25 declares a peer range of next@14 || 15, but it runs
# fine on next@16. Until the next-auth beta publishes an updated peer range,
# install with legacy peer resolution so npm doesn't refuse the install.
legacy-peer-deps=true
